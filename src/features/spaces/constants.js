import { HUE_KEYS } from '@/lib/tint'
import { DEFAULT_SPACE_EMOJI } from '@/components/shared/spaceEmoji'

/** Colour keys a space can use (tokens in index.css → --hue-<key>). */
export const SPACE_COLORS = HUE_KEYS

/** spaces.icon stores an emoji (not an icon key): user decision 2026-09-23. */
export const DEFAULT_SPACE_ICON = DEFAULT_SPACE_EMOJI
export const DEFAULT_SPACE_COLOR = 'blue'
